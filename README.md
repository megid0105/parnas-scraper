# Parnas Substack Scraper

## Setup

1. Fork or clone this repo.
2. Copy `.env.example` to `.env` and fill in:
   - `SUBSTACK_SID` (your Substack session cookie)
   - `WEBHOOK_URL` (your n8n webhook URL)

## Local Test

```bash
npm install
node index.js
